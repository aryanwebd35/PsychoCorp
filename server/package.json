{
  "name": "server",
  "version": "1.0.0",
  "description": "Express backend ",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^6.7.0",
    "mongoose": "^6.8.2",
    "nodemailer": "^6.9.1",
    "cors": "^2.8.5",             // ✅ Needed if frontend is on another domain (like Vercel)
    "dotenv": "^16.0.3",          // ✅ Load .env file (DB_URL, JWT_SECRET, etc.)
    "multer": "^1.4.5-lts.1"      // ✅ Required if uploading images (you used this in food add route
  }
}
